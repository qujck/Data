CREATE TABLE [Foundation].[Multiplicity] (
    [Value]       CHAR (1)      NOT NULL,
    [Description] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_Multiplicity] PRIMARY KEY CLUSTERED ([Value] ASC)
);

