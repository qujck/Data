CREATE TABLE [Foundation].[SqlResultType] (
    [Value]       VARCHAR (50)  NOT NULL,
    [Description] VARCHAR (100) NULL,
    CONSTRAINT [PK_SqlResultType] PRIMARY KEY CLUSTERED ([Value] ASC)
);

